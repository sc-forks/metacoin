version: 2.0

jobs:
  coverage:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: Install
          command: npm install
      - run:
          name: Coverage
          command: npx truffle run coverage
      - run:
          name: Coveralls
          command: bash coverage/lcov.info | npx coveralls

workflows:
  version: 2
  build:
    jobs:
      - coverage
