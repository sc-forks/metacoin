version: 2.0
aliases:
  - &defaults
    docker:
      - image: circleci/node:10

  - &install
    run:
      name: Install
      command: npm install

jobs:
  test:
    <<: *defaults
    steps:
      - checkout
      - *install
      - run:
          name: Test
          command: npx truffle test

  coverage:
    <<: *defaults
    steps:
      - checkout
      - *install
      - run:
          name: Coverage
          command: npx truffle run coverage

workflows:
  version: 2
  build:
    jobs:
      - test
      - coverage
